extends layout
block content
  section.hero.is-info.is-fullheight
    .hero-body
      .container
        h1.title
          | I am Yap Wei Jie Joshua
        h2.subtitle
          | Technology enthusiast
  //- ul
  //-   each q in quotes
  //-     li
  //-       span #{q.name} - #{q.quote}
